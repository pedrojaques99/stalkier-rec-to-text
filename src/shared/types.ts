/**
 * Os tipos moram em `@stalkier/core`, porque o Jaques Studio usa os mesmos.
 *
 * Este arquivo continua existindo pra que o resto do app importe de um lugar
 * estável: no dia em que algum tipo for só desta casca, ele nasce aqui sem
 * ninguém ter que reescrever import.
 */
export * from '@stalkier/core';
